services:
  - type: web
    name: aura-code-reviewer
    env: docker
    plan: free
    region: oregon
    dockerfilePath: Dockerfile
    dockerContext: .
    envVars:
      - key: PORT
        value: 8080
      - key: HOST
        value: 0.0.0.0
